node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo
.DS_Store
test-results/
playwright-report/

# Env files — NEVER commit secrets (this repo is public). Real keys live in
# .env.local, which is ignored here; .env.example is a committed, secret-free
# template.
.env
.env.*
!.env.example
